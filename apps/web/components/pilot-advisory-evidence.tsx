import { CircleHelp, Route, ShieldCheck } from "lucide-react";
import type { Advisory } from "@/lib/api";
import { Card } from "@/components/ui/card";
import { StatusBadge } from "@/components/ui/status-badge";
export function PilotAdvisoryEvidence({ advisory }: { advisory: Advisory }) { return <Card className="p-5 md:p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-teal">Pilot evidence</p><h3 className="mt-2 font-display text-2xl font-semibold">Evidence behind this advisory</h3></div><StatusBadge state={advisory.risk_state} label={advisory.risk_state.toUpperCase()} /></div><dl className="mt-5 grid gap-4 text-sm md:grid-cols-2"><Item icon={Route} label="Route state" value={advisory.route_state === "road_route_available" ? "Road route available" : "Road route unavailable"}/><Item icon={ShieldCheck} label="Locations checked" value={`${advisory.consignment.origin_location.name} → ${advisory.consignment.destination_location.name}`}/><Item icon={CircleHelp} label="Active review areas checked" value={`${advisory.considered_outbreak_ids?.length ?? 0} persisted pilot area(s)`}/><Item icon={ShieldCheck} label="Vaccination evidence" value={advisory.consignment.vaccination_evidence}/><Item icon={ShieldCheck} label="Coverage / rule engine" value={`${advisory.evidence_coverage_score}/100 · ${advisory.rules_version}`}/><Item icon={ShieldCheck} label="Generated" value={new Intl.DateTimeFormat("en-IN",{dateStyle:"medium",timeStyle:"short"}).format(new Date(advisory.evaluated_at))}/></dl><ol className="mt-5 space-y-2">{advisory.reasons.map(reason=><li key={reason.code} className="rounded-lg border border-line bg-paper p-3 text-sm">{reason.text}</li>)}</ol><p className="mt-5 rounded-lg border border-line bg-white p-4 text-sm font-semibold">This is decision support based on locally entered pilot data. An authorised veterinary official makes the final decision.</p></Card>; }
function Item({icon:Icon,label,value}:{icon:typeof Route;label:string;value:string}){return <div><dt className="flex items-center gap-2 text-xs font-bold uppercase text-slate-500"><Icon size={14} aria-hidden="true"/>{label}</dt><dd className="mt-1 font-semibold text-ink">{value}</dd></div>}
