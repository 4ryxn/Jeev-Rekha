"use client";
import{useCallback,useEffect,useState}from"react";import Link from"next/link";import{apiFetch,type ReviewCase}from"@/lib/api";import{Card}from"@/components/ui/card";import{Button}from"@/components/ui/button";import{DataContextLabel}from"@/components/data-context-control";import{sourcePath,useDataContext}from"@/lib/data-context";
export function ReviewQueue(){const{context}=useDataContext();const[cases,setCases]=useState<ReviewCase[]>([]),[selected,setSelected]=useState<ReviewCase|null>(null),[note,setNote]=useState("");const load=useCallback(()=>apiFetch<ReviewCase[]>(sourcePath("/review-cases",context)).then(setCases),[context]);useEffect(()=>{load()},[load]);const pilot=(x:ReviewCase)=>x.source_type==="advisory"&&x.data_source==="pilot_entered";const update=async(status:"acknowledged"|"resolved")=>{if(!selected)return;setSelected(await apiFetch<ReviewCase>(`/review-cases/${selected.id}`,{method:"PATCH",body:JSON.stringify({status,resolution_note:note})}));load()};return <div className="space-y-6"><DataContextLabel/><p className="rounded border border-line bg-paper p-4 text-sm">Review decisions organise evidence for authorised follow-up; they do not alter the source record.</p><Card className="divide-y">{cases.map(x=><button key={x.id} onClick={()=>setSelected(x)} className="block w-full p-4 text-left hover:bg-paper"><b>{x.priority} priority · {x.title}</b>{pilot(x)&&<span className="ml-2 rounded-full border border-line px-2 py-1 text-xs font-bold">Pilot advisory</span>}<p className="text-sm">{x.summary}</p></button>)}{!cases.length&&<p className="p-5">No persisted review cases match this data context.</p>}</Card>{selected&&<Card className="p-5"><h2 className="text-xl font-bold">{selected.title}</h2>{pilot(selected)&&<p className="mt-2 text-sm font-bold">Pilot advisory</p>}<p className="mt-2">{selected.source_summary}</p><Link className="mt-3 inline-block text-teal underline" href={selected.source_href}>{pilot(selected)?"Open pilot advisory evidence":"Open original evidence"}</Link>{selected.status==="open"&&<Button className="mt-3" onClick={()=>update("acknowledged")}>Acknowledge case</Button>}{selected.status!=="resolved"&&<div className="mt-3"><textarea aria-label="Resolution note" value={note} onChange={e=>setNote(e.target.value)} className="block w-full border"/><Button className="mt-2" disabled={!note.trim()} onClick={()=>update("resolved")}>Resolve case</Button></div>}</Card>}</div>}
