"use client";import{useEffect,useState}from"react";import Link from"next/link";import{apiFetch,type ReportIndex}from"@/lib/api";import{Card}from"@/components/ui/card";import{DataContextLabel}from"@/components/data-context-control";import{DataSourceBadge}from"@/components/data-source-badge";import{sourcePath,useDataContext}from"@/lib/data-context";export function ReportsHome(){const{context}=useDataContext();const[r,setR]=useState<ReportIndex|null>(null);useEffect(()=>{apiFetch<ReportIndex>(sourcePath('/reports',context)).then(setR)},[context]);if(!r)return <p>Loading persisted report sources…</p>;const groups=[['Movement Advisory Brief',r.advisories],['Trace Contact Review Package',r.traces],['Containment Action Brief',r.containment]] as const;return <div className="space-y-5"><DataContextLabel/><p className="rounded border border-line bg-paper p-4 text-sm">Synthetic data only. These browser-printable briefs are decision support, not government certificates or orders.</p>{groups.map(([title,items])=><Card key={title} className="p-5"><h2 className="font-display text-xl font-bold">{title}</h2>{items.length?<ul className="mt-3 space-y-2">{items.map(x=><li className="flex items-center gap-2" key={x.id}><Link className="text-teal underline" href={x.href}>{x.title}</Link>{context==='all'&&<DataSourceBadge source={x.data_source}/>}</li>)}</ul>:<p className="mt-3 text-sm">No persisted source is available yet.</p>}</Card>)}</div>}
