"use client";
import { apiFetch, type Location } from "@/lib/api";
import { offlineDb } from "@/lib/offline-queue";
export async function loadLocationReferences(source?: "demo_seed" | "pilot_entered"): Promise<Location[]> { try { const locations=await apiFetch<Location[]>("/locations",{cache:"no-store"}); if(locations.length<2)throw new Error("Location references are incomplete"); await offlineDb.locationReferences.bulkPut(locations); const filtered=source?locations.filter(x=>x.data_source===source):locations; if(!filtered.length)throw new Error("No active locations are available for this record context."); return filtered; } catch { const locations=await offlineDb.locationReferences.orderBy("name").toArray() as Location[]; const filtered=source?locations.filter(x=>x.data_source===source):locations; if(!filtered.length)throw new Error("Connect once to load local location references before offline registration."); return filtered; } }
