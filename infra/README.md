# Infrastructure

Local infrastructure is limited to PostgreSQL with PostGIS via the root `docker-compose.yml` during Phase 1. No application schema, migrations, or deployment configuration has been created.
